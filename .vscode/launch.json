{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Vulkan Guide",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/bin/vulkan_guide",
      "args": [],
      "cwd": "${workspaceFolder}",
      "environment": [
        {
          "name": "DYLD_LIBRARY_PATH",
          "value": "/usr/local/lib"
        }
      ]
    }
  ]
}